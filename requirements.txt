# Flask (minimal dependencies, works fully offline)
flask==3.1.0
waitress==3.0.1      # Windows WSGI server (replaces uvicorn, no C compiler needed)

# Scientific Computing (same as before)
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.1
pandas==2.2.2

# Packaging
pyinstaller==6.9.0

# NOTE: sqlite3 is built into Python — no install needed
# NOTE: threading is built into Python — no install needed
# NOTE: jinja2 comes with flask — no separate install needed
