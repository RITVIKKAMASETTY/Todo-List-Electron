# Flask (minimal dependencies, works fully offline)
flask
waitress

# Scientific Computing
# Versions below have pre-built wheels for Python 3.10, 3.11, 3.12, AND 3.13
numpy>=1.26
scipy>=1.14
scikit-learn>=1.5
pandas>=2.2

# Packaging (only needed to build the .exe)
pyinstaller
